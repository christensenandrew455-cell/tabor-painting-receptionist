{
  "name": "tabor-painting-receptionist",
  "version": "6.0.0",
  "description": "Telnyx and OpenAI Realtime receptionist with ARC-backed business knowledge and confirmed estimate intake.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "for file in *.js test/*.js; do node --check \"$file\" || exit 1; done",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
