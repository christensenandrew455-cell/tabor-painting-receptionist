{
  "name": "tabor-painting-receptionist",
  "version": "3.0.0",
  "description": "Multi-tenant Telnyx and OpenAI Realtime receptionist that resolves each business by its inbound phone number.",
  "main": "multi-tenant-server.js",
  "type": "module",
  "scripts": {
    "start": "node multi-tenant-server.js",
    "dev": "node --watch multi-tenant-server.js",
    "check": "node --check call-policy.js && node --check receptionist-core.js && node --check server.js && node --check tenant-profile.js && node --check multi-tenant-server.js",
    "test": "node --import ./test/test-env.js --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
